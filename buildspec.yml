version: 0.2

env:
    variables:
        BUILD_FOLDER: "dist"
phases:
  install:
    commands:
        - echo Installing source NPM dependencies...
        - cd littleFriends
        - npm install
        - npm install -g @angular/cli
  build:
    commands:
        - echo Build started
        - ls
        - ng build
artifacts:
    files:
        - '**/*'
    base-directory: 'littleFriends/dist*'
    discard-paths: yes
